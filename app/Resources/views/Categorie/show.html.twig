{% extends "::base1.html.twig" %}
{% block body %}
<div class="content-wrapper">
<br>
    <br>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">Categorie</h3>
                </div>
                <div class="panel-body">
                    <table class="table table-bordered table-hover">
                        <tbody>
                            <tr>
                                <th>Id</th>
                                <td>{{ categorie.id }}</td>
                            </tr>
                            <tr>
                                <th>Nom</th>
                                <td>{{ categorie.nom }}</td>
                            </tr>

                        </tbody>
                    </table>
                   <div class="form-group">
                <div class="col-md-offset-2 col-md-12">
                    <a href="{{ path('categorie_edit', { 'id': categorie.id }) }}" class="btn btn-primary"><span class="glyphicon glyphicon-edit"></span> Modifier</a>
                    <a href="{{ path('categorie_index') }}" class="btn btn-success"><span class="glyphicon glyphicon-backward"></span> Retourner vers la liste</a>
                </div>
            </div>         
                </div>
            </div>
        </div>
    </div>    </div>





{% endblock %}
